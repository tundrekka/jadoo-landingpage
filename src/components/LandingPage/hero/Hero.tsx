import { Decore } from "./Decore"
import { HeroContent } from "./HeroContent"

export const Hero: React.FC = () => {
   return (
      <section>
         <Decore />
         <HeroContent />
      </section>
   )
}